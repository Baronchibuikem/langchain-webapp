To run

    pip install -r requirements.txt

Open two(2) terminals and run the following command in each

    <!-- first terminal run -->
    make run-server

    <!-- second terminal run -->
    make run-client
    